// en este archivo, uniremos firebase.js y todos sus metodos, con context.js

import firebase from './firebase';
import FirebaseContext from '../firebase/context';

export { FirebaseContext }

export default firebase;